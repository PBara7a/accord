import { Grid } from "@mui/material";
import ChatHeader from "./ChatHeader";

function Main() {
  return (
    <Grid container padding={2}>
      <ChatHeader />
    </Grid>
  );
}

export default Main;
